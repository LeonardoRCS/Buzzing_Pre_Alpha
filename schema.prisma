datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model BusTrip {
  id             String   @id @default(uuid())
  lineName       String
  scheduledTime  DateTime
  status         String
}
